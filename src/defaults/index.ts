import CLASSNAMES from './elementOptions/classNames';
import COMBINATORS from './elementOptions/combinators';
import OPERATORS from './elementOptions/operators';
import RULE from './ruleElements';
import RULE_GROUP from './ruleGroupElements';

export const Defaults = {
  RULE,
  RULE_GROUP,
  OPERATORS,
  COMBINATORS,
  CLASSNAMES,
};

export default Defaults;
